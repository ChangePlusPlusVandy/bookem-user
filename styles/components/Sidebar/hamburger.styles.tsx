import Image from 'next/image';
import Link from 'next/link';
import styled from 'styled-components';

export const Hamburger = styled(Image)``;

export const HamburgerContainer = styled(Link)`
  position: absolute;
  right: 0;
  margin: 100px 40px 0 0;
`;
